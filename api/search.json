[{"id":"7897b3681b57eabb4fa728c873d77f49","title":"工厂模式学习","content":"前言最近的项目正在搭建框架，本科的时候做大创写代码都没有考虑到设计模式的问题，代码一多起来了，改动和拓展的成本就很大，也不方便理解整个项目的逻辑。因为我自己没有用过什么设计模式，对于我目前要做的项目要用什么样的设计模式我没法自己构思。但是我看过不少类似我现在项目的项目如果使用了设计模式一般都是工厂模式，当然还看过一个学长用建造者模式构建过。所以目前打算先学习一下工厂模式和建造者模式，再结合我的项目进行具体的代码编写。\n工厂模式工厂模式（Factory Pattern）是一种创建对象的设计模式，属于创建型模式之一。它提供了一种统一的接口来创建对象，但具体的对象的实例化过程由工厂类来决定。工厂模式可以隐藏对象的实例化逻辑，使得代码更加灵活、可扩展和易于维护。核心思想是将对象的实例化过程封装在一个工厂类中。该工厂类负责根据参数或配置信息的不同，返回不同的具体对象实例。通过使用工厂类，客户端代码可以通过调用工厂方法来获取所需的对象，而无需了解对象的具体创建过程。\n简单工厂模式简单工厂模式（Simple Factory Pattern）是工厂模式的一种简化形式，也被称为静态工厂模式。它通过一个工厂类，根据客户端传递的参数或条件来创建不同的产品对象。下面是一个简单工厂模式的示例，假设我们有不同类型的几何图形，包括圆形和矩形。我们可以使用简单工厂模式创建这些几何图形的对象。首先，我们定义一个抽象的几何图形接口或抽象类，它包含一个用于计算面积的方法：\n# 抽象产品：几何图形接口\nclass Shape:\n    def calculate_area(self):\n        pass\n\n​\t然后，我们定义具体的几何图形类，实现抽象几何图形接口：\n# 具体产品：圆形类\nclass Circle(Shape):\n    def __init__(self, radius):\n        self.radius = radius\n\n    def calculate_area(self):\n        return 3.14 * self.radius * self.radius\n\n# 具体产品：矩形类\nclass Rectangle(Shape):\n    def __init__(self, length, width):\n        self.length = length\n        self.width = width\n\n    def calculate_area(self):\n        return self.length * self.width\n\n接下来，我们定义一个几何图形工厂类，负责根据客户端传递的参数创建对应的几何图形对象：\n# 简单工厂：几何图形工厂类\nclass ShapeFactory:\n    @staticmethod\n    def create_shape(shape_type, *args):\n        if shape_type == \"circle\":\n            return Circle(*args)\n        elif shape_type == \"rectangle\":\n            return Rectangle(*args)\n        else:\n            return None\n\n\n最后，我们可以使用几何图形工厂来创建不同类型的几何图形对象，而无需直接与具体的几何图形类进行交互：\n# 客户端代码\nfactory = ShapeFactory()\n\ncircle = factory.create_shape(\"circle\", 5)\nprint(circle.calculate_area())  # 输出：78.5\n\nrectangle = factory.create_shape(\"rectangle\", 3, 4)\nprint(rectangle.calculate_area())  # 输出：12\n\n通过简单工厂模式，我们可以通过调用工厂类的静态方法来创建不同类型的几何图形对象，而无需了解具体的创建过程。这样可以简化对象的创建过程，并提高代码的可读性和可维护性。\n需要注意的是，简单工厂模式违反了开放-封闭原则，因为每次添加新的产品类型时，都需要修改工厂类的代码。如果需要支持更多类型的产品，可能需要考虑使用工厂方法模式或抽象工厂模式来提供更好的扩展性。\n工厂方法模式开闭原则（Open-Closed Principle）是面向对象设计的重要原则之一，它指导我们编写的软件实体（类、模块、函数等）应该对扩展开放，对修改关闭。换句话说，我们应该通过扩展现有的实体来实现功能的增加，而不是直接修改现有的实体代码。为了遵循开闭原则，可以考虑使用工厂方法模式或抽象工厂模式。这些模式提供了更好的扩展性，可以轻松地添加新的产品类型，而无需修改现有的工厂类代码。首先，我们定义一个抽象的几何图形抽象类，它包含几何图形的共同方法：\n# 抽象产品：几何图形接口\nclass Shape:\n    def calculate_area(self):\n        pass\n\n然后，我们为每种几何图形定义具体的类，实现抽象几何图形接口：\n# 具体产品：圆形类\nclass Circle(Shape):\n    def __init__(self, radius):\n        self.radius = radius\n\n    def calculate_area(self):\n        return 3.14 * self.radius * self.radius\n\n# 具体产品：矩形类\nclass Rectangle(Shape):\n    def __init__(self, length, width):\n        self.length = length\n        self.width = width\n\n    def calculate_area(self):\n        return self.length * self.width\n\n# 具体产品：三角形类\nclass Triangle(Shape):\n    def __init__(self, base, height):\n        self.base = base\n        self.height = height\n\n    def calculate_area(self):\n        return 0.5 * self.base * self.height\n\n\n接下来，我们定义一个抽象的几何图形工厂接口，其中包含一个用于创建几何图形对象的工厂方法：\n# 抽象工厂：几何图形工厂接口\nclass ShapeFactory:\n    def create_shape(self):\n        pass\n\n然后，为每种几何图形定义具体的工厂类，实现几何图形工厂接口，分别负责创建对应类型的几何图形对象：\n# 具体工厂：圆形工厂类\nclass CircleFactory(ShapeFactory):\n    def create_shape(self):\n        radius = float(input(\"请输入圆的半径：\"))\n        return Circle(radius)\n\n# 具体工厂：矩形工厂类\nclass RectangleFactory(ShapeFactory):\n    def create_shape(self):\n        length = float(input(\"请输入矩形的长度：\"))\n        width = float(input(\"请输入矩形的宽度：\"))\n        return Rectangle(length, width)\n\n# 具体工厂：三角形工厂类\nclass TriangleFactory(ShapeFactory):\n    def create_shape(self):\n        base = float(input(\"请输入三角形的底边长度：\"))\n        height = float(input(\"请输入三角形的高度：\"))\n        return Triangle(base, height)\n\n最后，我们可以使用具体的工厂类来创建对应类型的几何图形对象，而无需直接与具体的几何图形类进行交互：\n# 客户端代码\ncircle_factory = CircleFactory()\ncircle = circle_factory.create_shape()\nprint(\"圆的面积：\", circle.calculate_area())\n\nrectangle_factory = RectangleFactory()\nrectangle = rectangle_factory.create_shape()\nprint(\"矩形的面积：\", rectangle.calculate_area())\n\ntriangle_factory = TriangleFactory()\ntriangle = triangle_factory.create_shape()\nprint(\"三角形的面积：\", triangle.calculate_area())\n\n抽象工厂模式首先，我们定义一个抽象的几何图形接口或抽象类，它包含一个用于计算面积的方法：\n# 抽象产品：几何图形接口\nclass Shape:\n    def calculate_area(self):\n        pass\n\n然后，我们为每种几何图形定义具体的类，实现抽象几何图形接口：\n# 具体产品：圆形类\nclass Circle(Shape):\n    def __init__(self, radius):\n        self.radius = radius\n\n    def calculate_area(self):\n        return 3.14 * self.radius * self.radius\n\n# 具体产品：矩形类\nclass Rectangle(Shape):\n    def __init__(self, length, width):\n        self.length = length\n        self.width = width\n\n    def calculate_area(self):\n        return self.length * self.width\n\n# 具体产品：三角形类\nclass Triangle(Shape):\n    def __init__(self, base, height):\n        self.base = base\n        self.height = height\n\n    def calculate_area(self):\n        return 0.5 * self.base * self.height\n\n接下来，我们定义一个抽象的几何图形工厂接口，其中包含多个用于创建不同类型几何图形对象的工厂方法：\n# 抽象工厂：几何图形工厂接口\nclass ShapeFactory:\n    def create_circle(self):\n        pass\n\n    def create_rectangle(self):\n        pass\n\n    def create_triangle(self):\n        pass\n\n然后，为每种几何图形定义具体的工厂类，实现几何图形工厂接口，分别负责创建对应类型的几何图形对象：\n# 具体工厂：具体的几何图形工厂类\nclass ConcreteShapeFactory(ShapeFactory):\n    def create_circle(self, radius):\n        return Circle(radius)\n\n    def create_rectangle(self, length, width):\n        return Rectangle(length, width)\n\n    def create_triangle(self, base, height):\n        return Triangle(base, height)\n\n最后，我们可以使用具体的工厂类来创建对应类型的几何图形对象，而无需直接与具体的几何图形类进行交互：\n# 客户端代码\nfactory = ConcreteShapeFactory()\n\ncircle = factory.create_circle(5)\nprint(\"圆的面积：\", circle.calculate_area())\n\nrectangle = factory.create_rectangle(3, 4)\nprint(\"矩形的面积：\", rectangle.calculate_area())\n\ntriangle = factory.create_triangle(3, 6)\nprint(\"三角形的面积：\", triangle.calculate_area())\n\n下面是对简单工厂模式、工厂方法模式和抽象工厂模式的优点和缺点的总结：\n简单工厂模式的优点：\n\n将对象的创建集中在一个工厂类中，降低了客户端代码与具体产品类之间的耦合度。\n对象的创建逻辑被封装在工厂类中，客户端无需知道具体产品的创建细节。\n\n简单工厂模式的缺点：\n\n当需要添加新的产品类型时，需要修改工厂类的代码，违反了开闭原则。\n工厂类负责创建不同类型的产品，职责较重，不符合单一职责原则。\n\n工厂方法模式的优点：\n\n每个具体产品都有对应的工厂类，符合单一职责原则。\n新增产品类型时，只需添加对应的具体产品类和具体工厂类，不需要修改现有代码，符合开闭原则。\n客户端通过与抽象工厂接口交互，无需关心具体的产品类，实现了对象的创建与使用的解耦。\n\n工厂方法模式的缺点：\n\n每个具体产品都需要对应一个具体工厂类，增加了类的个数，导致类的结构复杂化。\n客户端需要知道具体工厂类的存在，与具体工厂类之间存在依赖关系。\n\n抽象工厂模式的优点：\n\n提供了一种接口或抽象类来创建一组相关的产品对象，符合开闭原则。\n客户端通过与抽象工厂接口交互，无需关心具体的产品类和工厂类，实现了对象的创建与使用的解耦。\n可以方便地替换具体工厂类，实现不同产品族的切换。\n\n抽象工厂模式的缺点：\n\n增加了系统的复杂度，因为需要定义多个抽象产品和抽象工厂，以及对应的具体产品和具体工厂。\n当需要添加新的产品族时，需要修改抽象工厂接口及其所有的具体工厂类，不符合开闭原则。\n\n总结起来，简单工厂模式适用于简单的对象创建场景，但违反了开闭原则和单一职责原则；工厂方法模式通过定义工厂接口和具体工厂类，实现了对象的创建与使用的解耦，符合开闭原则和单一职责原则；抽象工厂模式适用于创建一组相关的产品对象的场景，提供了一种创建不同产品族的方式，但增加了系统的复杂度。选择适合的模式要根据具体的情况而定。\n","slug":"工厂模式学习","date":"2023-05-21T02:49:36.000Z","categories_index":"","tags_index":"设计模式","author_index":"弈梒"},{"id":"9766db887146dcc08804d8995ad331d6","title":"Welcome to Yihan Blog","content":"​\t欢迎来到弈梒blog，这里将会记录一些我的生活，目前还在搭建改造中。\n","slug":"hello-world","date":"2023-05-19T15:02:29.198Z","categories_index":"","tags_index":"","author_index":"弈梒"}]